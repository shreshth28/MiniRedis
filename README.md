# MiniRedis
MiniRedis is a minimal, Redis-like key-value store implemented from scratch  in modern C++17. It evolves from a simple REPL, into a TCP server, and  eventually into a concurrent, persistent storage engine with CI/CD and  cloud deployment.
